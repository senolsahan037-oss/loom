"""Measured musical evidence, one layer at a time.

Importable from two places on purpose: the source tree, where it sits beside
Sensei, and the extension's embedded runtime, where build.ts copies it next to
core/ so the same code runs inside Live without the rest of the repository.
"""
